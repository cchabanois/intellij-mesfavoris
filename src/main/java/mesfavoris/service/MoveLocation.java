package mesfavoris.service;

public enum MoveLocation {
    /** Move bookmarks into a folder */
    INTO,
    /** Move bookmarks before the target bookmark */
    BEFORE,
    /** Move bookmarks after the target bookmark */
    AFTER
}
